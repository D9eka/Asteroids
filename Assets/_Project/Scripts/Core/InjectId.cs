namespace Asteroids.Scripts.Core
{
    public enum InjectId
    {
        BoundsMargin,
        PlayerStartPos,
        PlayerTransform,
        PlayerCollisionService,
        PlayerCollisionHandler,
        PlayerBulletGun,
        PlayerLaserGun,
        PlayerWeapons
    }
}