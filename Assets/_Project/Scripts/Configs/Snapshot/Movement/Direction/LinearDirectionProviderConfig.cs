namespace Asteroids.Scripts.Configs.Snapshot.Movement.Direction
{
    public class LinearDirectionProviderConfig : DirectionProviderConfig
    {
        public LinearDirectionProviderConfig(float minSpeed, float maxSpeed) : base(minSpeed, maxSpeed)
        {
        }
    }
}